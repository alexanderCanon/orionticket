spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  rabbitmq:
    listener:
      simple:
        auto-startup: false
  scheduling:
    enabled: false

rabbitmq:
  queues:
    order-created: payments.queue.order-created.test
    date-added: payments.queue.date-added.test

stripe:
  api-key: sk_test_placeholder
  webhook-secret: whsec_placeholder

services:
  orders:
    base-url: http://localhost:18084
  event-management:
    base-url: http://localhost:18082
