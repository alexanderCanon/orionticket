spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/event_management_db
    username: orion
    password: secret
  flyway:
    url: jdbc:postgresql://localhost:5434/event_management_db
    user: orion
    password: secret
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
